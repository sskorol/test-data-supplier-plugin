buildscript {
    repositories {
        mavenCentral()
    }
}

plugins {
    id 'org.jetbrains.intellij' version '0.3.1'
}

group = 'io.github.sskorol'
version = '0.2.0'

apply plugin: 'idea'
apply plugin: 'org.jetbrains.intellij'
apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
    jcenter()
}

dependencies {
    compile 'io.github.sskorol:test-data-supplier:1.5.5'
}

task wrapper(type: Wrapper) {
    gradleVersion = '4.6'
}

intellij {
    version = 'IU-2018.1'
    pluginName = 'Test Data Supplier'
}

publishPlugin {
    String uName = project.hasProperty('uName') ? project.property('uName') : System.getenv('USERNAME')
    String uPass = project.hasProperty('uPass') ? project.property('uPass') : System.getenv('PASSWORD')

    username "${uName}"
    password "${uPass}"
}
